{
  "name": "qrew",
  "private": true,
  "workspaces": [
    "apps/app"
  ],
  "scripts": {
    "dev:browser": "npm run dev -w apps/app",
    "dev:android": "cd apps/app && npx vite --host",
    "android:emulator": "bash scripts/android-emulator.sh",
    "android:phone": "bash scripts/android-phone.sh",
    "build": "npm run build -w apps/app",
    "preview": "npm run preview -w apps/app",
    "lint": "npm run lint -w apps/app",
    "lint:fix": "npm run lint:fix -w apps/app",
    "format": "npm run format -w apps/app",
    "format:check": "npm run format:check -w apps/app",
    "test": "npm run test -w apps/app",
    "typecheck": "npm run typecheck -w apps/app"
  }
}
