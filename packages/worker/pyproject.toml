[project]
name = "worker"
version = "0.1.0"
description = "Qrew worker."
requires-python = ">=3.12"
dependencies = [
    "structlog>=24.0",
]

[tool.uv]
venvPath = "../.."
venv = ".venv"

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "strict"
include = ["src"]
