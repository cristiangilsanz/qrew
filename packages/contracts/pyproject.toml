[project]
name = "contracts"
version = "0.1.0"
description = "Qrew contracts."
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
]

[tool.uv.sources]

[tool.uv]
venvPath = "../.."
venv = ".venv"

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "strict"
include = ["src"]
