[project]
name = "middleware"
version = "0.1.0"
description = "Qrew middleware."
requires-python = ">=3.12"
dependencies = [
    "starlette>=0.40",
    "structlog>=24.0",
]

[tool.uv.sources]

[tool.uv]
venvPath = "../.."
venv = ".venv"

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "strict"
include = ["src"]
